//? controller book
//Todo list:
/* 
    ? Make the posting function (return: id so we can redirect it) ?
    * Make the edit function (return: id so we can redirect it) *
    ! Make the delete function (return: 200 so we can redirect it to / !
    ? Make the get function (return: bookType so we can show it)?
    ? Make the add comment(the id) function (return: 200 so it's just add it with frontend) ?
    * Make the get with user id function (return: bookType[] so we can show it) *
*/
//Good Luck :)